import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  root: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  pressable: {
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
});
